spring:
  application:
    name: spring-petclinic-rest
  cloud:
    consul:
      host: localhost
      port: 8500
      config:
        enabled: true
        discovery:
          healthCheckInterval: 300s
          instanceId: 'hola:2021'
